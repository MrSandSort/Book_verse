// Copy this file to `config.js` and fill in your own project's values.
// `config.js` is what index.html and admin.html actually load — keep
// your real anon key out of version control by adding config.js to
// .gitignore (a matching entry is already in the provided .gitignore).

window.VERSES_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
